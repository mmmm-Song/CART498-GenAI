prompt = (
    "surrealist found-image collage, symbolic juxtaposition,"
    "kid head, adult body,visible cut-paper join,"
    "Invoices,letters, bills, tax stamps stack on office desk, "
    "nineteenth-century engraving, full color, high contrast, cross-hatching, "
)

negative_prompt = (
     "blurry, lowres, watermark, text, deformed faces, covered face, extra limbs, "
    "bad anatomy, extra fingers, cartoon, oversaturated, modern"
)

image = pipe(
    prompt=prompt,
    negative_prompt=negative_prompt,
    generator=torch.Generator(device).manual_seed(1000), # change the seed!
    num_inference_steps=1, guidance_scale=0.0,
    height=512,
    width=512,
).images[0]
